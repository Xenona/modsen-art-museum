import styled from 'styled-components';

export const StubImage = styled.div`
  box-sizing: border-box;
  border: 1px solid var(--weak-accent-color);
  display: flex;
  align-items: center;
  justify-content: center;
  height: 100%;
`;
